name: check-secret
on: workflow_dispatch
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ANTHROPIC_API_KEY length: ${#KEY}"
          echo "starts with: ${KEY:0:8}"
        env:
          KEY: ${{ secrets.ANTHROPIC_API_KEY }}
