# Build artifacts
/dist/
/bin/
# The bundled skill binary is a build product; SKILL.md stays committed.
skills/social-fetch/scripts/social-fetch

# Local-only npm tooling (currently just @anthropic-ai/mcpb for
# validating the Claude Desktop Extension manifest). package.json +
# package-lock.json are committed; the install tree is not.
/node_modules/

# Go test outputs
*.test
*.out
coverage.out

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local environment — never commit credentials
.env
.env.*
**/.env
**/.env.*
*.local

# Project MCP server config — hardcoded host paths (dist/...) and
# operator-specific server choices. Each developer maintains their
# own. The repo's canonical advice for setting one up lives in
# INSTALL.md or per-feature commit messages.
.mcp.json

# Output / scratch
/out/
*.log
/social-fetch

# Bundled skill ledger binary — built fresh by `make skill-build`,
# same gitignored treatment as scripts/social-fetch.
skills/social-fetch/scripts/social-ledger
skills/social-ledger/scripts/social-ledger
/social-browser
/social-fetch
/social-ledger
